interface TourCardProps ;
  key?: string | number;
}


  const [isHovered, setIsHovered] = useState(false);
  const [showTooltip, setShowTooltip] = useState(false);

  
    <div> setIsHovered(true)}
      onMouseLeave=
      class="bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition-all group flex flex-col border border-slate-100 h-full relative"
    >
      
      <div class="relative h-56 overflow-hidden">
        <img 
          src= 
          alt= 
          class="w-full h-full object-cover group-hover:scale-110 transition-all duration-700" 
        />
        
        <i data-lucide="animate-presence" ></i>
          
        </i>

        
          </div>
        )}
        
        <button class="absolute top-4 right-4 bg-white/20 backdrop-blur-md p-2 rounded-full text-white hover:bg-white hover:text-red-500 transition-all z-20">
          <i data-lucide="heart" class="w-5 h-5" ></i>
        </button>
      </div>

      
      <div class="p-6 flex flex-col flex-grow">
        <div class="flex justify-between items-start mb-3">
          <h3 class="font-bold text-slate-800 leading-tight group-hover:text-primary transition-colors line-clamp-2">
            
          </h3>
          <div class="flex items-center gap-1 bg-green-50 px-2 py-1 rounded-lg shrink-0 ml-2">
            <i data-lucide="star" class="w-3 h-3 text-green-600 fill-green-600" ></i>
            <span class="text-green-700 font-bold text-xs"></span>
          </div>
        </div>

        
        <div class="relative mb-6">
          <div 
            class="flex items-center gap-2 text-slate-500 text-sm cursor-help w-fit"
            onMouseEnter=
            onMouseLeave=
          >
            <i data-lucide="clock" class="w-4 h-4 text-primary" ></i> 
            <span class="font-medium"></span>
          </div>
          
          <i data-lucide="animate-presence" ></i>
            
          </i>
        </div>

        <div class="mt-auto flex items-center justify-between pt-4 border-t border-slate-50">
          <div>
            <span class="text-[10px] text-slate-400 font-bold uppercase block">Starting from</span>
            <span class="text-2xl font-black text-primary">$</span>
          </div>
          <button class="bg-primary text-white px-5 py-2.5 rounded-xl font-bold text-xs hover:bg-primary-dark transition-all transform hover:scale-105 active:scale-95 shadow-md hover:shadow-lg">
            Details
          </button>
        </div>
      </div>
    </div>